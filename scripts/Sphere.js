// function Sphere (x, y, width, height, )

// // Draw a circle particle on the canvas
// context.beginPath();
// context.fillStyle = "white";
// // After setting the fill style, draw an arc on the canvas
// context.arc(500, 200, 10, 0, Math.PI*2, true); 
// context.closePath();
// context.fill();